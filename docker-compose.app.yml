services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
      - .env.docker
    environment:
      - OTEL_SERVICE_NAME=pipeline-app
    ports:
      - "5173:5173"
    depends_on:
      - redis
      - mongodb

  workers:
    build:
      dockerfile: workers/Dockerfile
    env_file:
      - .env
      - .env.docker
    environment:
      - OTEL_SERVICE_NAME=pipeline-workers
    depends_on:
      - redis
      - mongodb
